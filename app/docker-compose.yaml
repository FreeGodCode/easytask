version: "3"
services:
  easytaskappserver:
    image: easytaskappserver
    container_name: easytaskappserver
    restart: always
    network_mode: bridge
    volumes:
      - /webapps:/home/app
    ports:
      - "5006:5001"
    
